link(rel='stylesheet', href='/stylesheets/teststyle.css')

tupper

 header
  logo  
  u_twit_top 
   script(type='text/javascript')
    twttr.anywhere(function (T) {
     var currentUser,
      screenName,
      profileImage,
      profileImageTag;
     if (T.isConnected()) {
      currentUser = T.currentUser;
      screenName = currentUser.data('screen_name');
      profileImage = currentUser.data('profile_image_url');
      profileImageTag = "<img src='" + profileImage + "'/>";
      $('#twitter-connect-placeholder').append("Logged in as " + profileImageTag + " <heavy>" + screenName + currentUser);
     } 
     else {
      T("#twitter-connect-placeholder").connectButton();
     };  
    });
   span#twitter-connect-placeholder
  search
   input 
    placeholder search for a @username

 main_section
  userprime userprime
   u_twit_info u_twit_info
   u_self_info u_self_info
 mainblock
  h2 People 
   heavy @username 
   \wants to work with:
  ul#crushes
   li.crushes	 
    a.cun(href='http://www.twitter.com/username') @username
    span.cinfo \ bio from twitter
    p.unote whatever the user has to say about their crush
   li.crushes
    a.cun(href='http://www.twitter.com/username') @username
    span.cinfo \ bio from twitter
    p.unote whatever the user has to say about their crush
   li.crushes
    a.cun(href='http://www.twitter.com/username') @username
    span.cinfo \ bio from twitter
    p.unote whatever the user has to say about their crush
   li.crushes
    a.cun(href='http://www.twitter.com/username') @username
    span.cinfo \ bio from twitter
    p.unote whatever the user has to say about their crush
   button.crush Add
  button.tell Tell
  tweetbox tweetbox
   field field
   button.share Share
 footer
  ul
   li
    a(href='#about') About
   li 
    a(href='#tnc') Terms and Conditions